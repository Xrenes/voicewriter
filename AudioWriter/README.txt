AudioWriter logo and original entrance animation

Logo: SVG and PDF (vector); transparent PNG sizes 16-1024; multi-size Windows ICO; white-background JPG.
Animation: entrance SVG plays once; loading SVG loops; HTML includes preview and controls. GIF loops on a pale background; MP4 plays once on a pale background. SVG/HTML retain transparency. Reduced-motion settings are respected by SVG/HTML.
The logo is the clean vector reconstruction used in the animation preview, with rounded stroke ends; it is not a pixel-exact trace of the generated concept. Video/GIF timing is an approximation of CSS easing.
